apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: bridge-service-stack

commonLabels:
  app.kubernetes.io/name: bridge-gateway
  app.kubernetes.io/part-of: bridge-ecosystem

resources:
  - deployment.yaml
  - service.yaml
  # ingress.yaml excluded — managed by consolidated bridge-app-ingress
  - configmap.yaml
  - serviceaccount.yaml
