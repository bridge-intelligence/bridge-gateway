apiVersion: v1
kind: Service
metadata:
  name: bridge-gateway
  labels:
    app: bridge-gateway
    component: gateway
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 4000
      targetPort: 4000
      protocol: TCP
  selector:
    app: bridge-gateway
    component: gateway
